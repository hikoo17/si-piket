@extends('layouts.app') @section('content')<div class="flex justify-between"><h1 class="text-3xl font-bold">Kelas</h1><a href="{{ route('classes.create') }}" class="rounded bg-indigo-700 px-4 py-2 text-white">Tambah</a></div><div class="mt-5 rounded bg-white">@foreach($classes as $class)<div class="flex justify-between border-b p-4"><span>{{ $class->name }} · {{ $class->school->name }}</span><span><a class="text-indigo-700" href="{{ route('classes.edit',$class) }}">Edit</a> <form class="inline" method="POST" action="{{ route('classes.destroy',$class) }}">@csrf @method('DELETE')<button class="text-red-700">Hapus</button></form></span></div>@endforeach</div>{{ $classes->links() }}@endsection
