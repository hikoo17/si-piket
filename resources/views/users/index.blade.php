@extends('layouts.app') @section('content')<div class="flex justify-between"><h1 class="text-3xl font-bold">Pengguna</h1><a href="{{ route('users.create') }}" class="rounded bg-indigo-700 px-4 py-2 text-white">Tambah</a></div><div class="mt-5 overflow-auto rounded bg-white"><table class="w-full"><thead><tr class="border-b text-left"><th class="p-3">Nama</th><th>Role</th><th>Kelas</th><th>Aksi</th></tr></thead><tbody>@foreach($users as $user)<tr class="border-b"><td class="p-3">{{ $user->name }}<small class="block">{{ $user->email }}</small></td><td>{{ strtoupper($user->role) }}</td><td>{{ $user->schoolClass?->name ?? '-' }}</td><td><a href="{{ route('users.edit',$user) }}" class="text-indigo-700">Edit</a> <form class="inline" method="POST" action="{{ route('users.destroy',$user) }}">@csrf @method('DELETE')<button class="text-red-700">Hapus</button></form></td></tr>@endforeach</tbody></table></div>{{ $users->links() }}@endsection
